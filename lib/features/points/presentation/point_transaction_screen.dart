import 'package:flutter/material.dart';

class PointTransactionScreen extends StatelessWidget{
  const PointTransactionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}